import { DistanceDirective } from "./distance.directive";


describe('DistanceDirective', () => {
  it('should create an instance', () => {
    const directive = new DistanceDirective();
    expect(directive).toBeTruthy();
  });
});
